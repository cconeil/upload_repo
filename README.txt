This script uses Fabric (www.fabfile.org) to send and update files to a github repository.  It will upload an entire directory (selected by the user) to their selected repository.

To upload a new repo:
	